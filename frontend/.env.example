# Base URL of the backend API (see backend/.env.example / backend/app/config/settings.py
# for the corresponding CORS_ORIGINS setting, which must include this frontend's own origin).
VITE_API_BASE_URL=http://localhost:8000
